# 7 分钟 Demo 与代码讲解提纲

这份文档不是另一个 README，而是候选人的口头讲解脚本。先理解因果链，再记术语。

## 0:00–0:40｜问题定义

> VLA 一次生成未来 2.5 秒、50 个六轴绝对关节目标，但神经网络输出没有确定性
> 安全保证。我在 VLA 和机器人之间加入 SafeMotion：尽量保持原动作，但只有能够
> 用模型验证安全的速度才交给 Mock Robot；不能确认安全就停止。

强调三点：输入是绝对关节角；以 20 Hz 闭环执行；验收对象是实际执行轨迹。

## 0:40–1:20｜现场运行测试

```bash
pytest -q
```

不要只说“22 个测试通过”。解释测试覆盖五条不变量：

1. 非法输入不进入机器人；
2. 输出速度始终有限；
3. Mock Robot 永远不突破关节范围；
4. 每个实际状态的完整运动链都在 workspace 内；
5. 过滤器失败时执行零速度，而不是原始 VLA 动作。

## 1:20–2:10｜50 点闭环

指着 `safe_motion/replay.py` 解释：

```text
实际 q → 当前绝对目标 → 名义速度 → 安全过滤 → robot.step
        ←──────────── 下一点重新读取实际 q ────────────┘
```

上一动作可能被缩放，所以不能用两个 VLA 目标之差，也不能假设上一目标已经到达。

## 2:10–3:10｜FK 与全身安全

指着 `kinematics.py`：标准 DH 矩阵同时表达旋转和平移，六次相乘得到各关节和 TCP
相对于 base 的坐标。

指着 `geometry.py`：检查对象是活动关节以及相邻节点形成的连杆，而不是仅 TCP。
矩形盒是凸集，所以在题目的零厚度线段模型下，一根连杆两端在盒内就意味着整段
在盒内；这是解析结论，不存在 2–5 cm 采样漏检。

说明 base 约定：固定 pedestal 在 `z=0`，示例 keep-in 从 `z=0.05` 开始，因此默认
从 `joint_1` 检查活动链。它是可配置且有文档的模型边界。

## 3:10–4:10｜安全过滤

指着 `safety_filter.py`：先尝试 100% 名义速度，然后 75%、50%、25%、0%。每个
候选命令在 0.05 秒控制周期内取 10 个子步，逐个做关节限制、FK 和全身检查，选择
最大的安全比例。

工程取舍：它不是全局规划器，只沿原方向减少动作；优点是确定、保守、可解释、
无外部 solver，失败路径容易测试。持续停止时应反馈给上层重新规划。

## 4:10–5:30｜演示真实 mid-link 场景

```bash
python -m safe_motion.replay scenarios/real_03_tcp_safe_mid_link_unsafe.json \
  --output artifacts/real_03_replay.json --plot artifacts/real_03.png
```

官方参考数据中第一帧不安全发生在 step 46。最终名义状态中：

- TCP 的 workspace margin 仍为正，约 35 mm；
- 中间节点越过 `x_min`，约 61 mm；
- 只检查 TCP 会放行；全身检查会拦截；
- 本实现从 step 46 开始干预，实际状态的最小裕量始终非负。

打开 `artifacts/real_03.png`：红色虚线机械臂是名义危险姿态，蓝色是实际安全姿态，
黄色点是第一次干预。

## 5:30–6:20｜解释一次干预

从 `artifacts/real_03_replay.json` 选 step 46，顺序讲：

```text
q_before
→ q_target
→ (target-q)/dt
→ 速度裁剪后的 q_dot_nominal
→ 子步 FK 发现中间节点越过 x_min
→ 逐级缩放
→ q_dot_safe（或零速度）
→ Mock Robot 更新
→ 执行后再次验证
```

不要背六个浮点数；讲清数据怎样流过安全链路。

## 6:20–7:00｜能力边界

> 当前保证只成立于标称 DH、零厚度连杆、完美跟踪、固定周期和零延迟 Mock Robot。
> 它验证的是软件运动学过滤，不是真机功能安全认证。真机还需加入标定和跟踪误差
> 裕量、胶囊几何、动力学及制动距离、通信 watchdog、protective stop、急停和认证。

## 高频追问短答

**为什么不用 QP/CBF？** 题目允许缩放方案。两天原型首先保证正确、可测、可解释；
接口可在未来替换为 QP，但不能让复杂 solver 掩盖 fail-safe。

**0% 为什么有意义？** 它验证保持当前安全状态是否安全，并明确产生零速度。若当前
状态一开始就不安全，Replay 直接拒绝，因为停住并不能恢复一个已经越界的机器人。

**为什么要控制周期子步？** 只检查终点可能漏掉关节空间直线路径中的中间危险姿态；
子步降低这种离散漏检。它仍不是连续时间形式化证明，这是当前实现的限制。

**为什么 full-body 只查节点？** 只对本题的凸矩形盒和零厚度直线连杆成立；若加入
非凸障碍物或实体半径，就需要线段求交、胶囊距离或碰撞库。

**Agent 写了多少？** 可以坦诚说 AI 加速了脚手架、测试和文档，但你用官方三个真实
场景验证了因果链，能够解释每个安全不变量、设计取舍和能力边界。
